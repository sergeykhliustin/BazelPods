name: Tests
on: [push, pull_request]
jobs:
  test:
    name: Tests
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: brew install bazelisk
        run: brew install bazelisk
      - name: make prepare-tests tests
        run: make prepare-tests tests
